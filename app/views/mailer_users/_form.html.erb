<%= form_with(model: mailer_user, local: true) do |form| %>
  <% if mailer_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mailer_user.errors.count, "error") %> prohibited this mailer_user from being saved:</h2>

      <ul>
      <% mailer_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :mailer_user_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :mailer_user_email %>
  </div>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :mailer_user_login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
